import styled from 'styled-components';

const SubHeader = styled.div`
  font-family: Muli;
  font-style: normal;
  font-weight: 800;
  font-size: 27px;
  line-height: normal;
  color: #3d2f3d;
`;

export default SubHeader;
