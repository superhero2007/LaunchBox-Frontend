import styled from 'styled-components';

const Date = styled.span`
  font-family: Muli;
  font-style: normal;
  font-weight: bold;
  font-size: 13px;
  line-height: normal;
  text-transform: uppercase;
  opacity: 0.7;
`;

export default Date;
