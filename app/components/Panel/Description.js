import styled from 'styled-components';

const Description = styled.span`
  margin-top: 22px;
  font-family: Muli;
  font-style: normal;
  font-weight: normal;
  font-size: 15px;
  line-height: 23px;
`;

export default Description;
