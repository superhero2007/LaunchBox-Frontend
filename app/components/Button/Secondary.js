import styled from 'styled-components';

const Secondary = styled.button`
  font-family: Muli;
  font-style: normal;
  font-weight: normal;
  font-size: 12px;
  line-height: normal;
  letter-spacing: 0.07em;
  text-transform: uppercase;
  color: rgba(150, 125, 135, 0.7);
  width: 60px;
  border-radius: 7px;

  &:hover {
    color: white;
    background: rgba(150, 125, 135, 0.7);
  }
`;

export default Secondary;
