import styled from 'styled-components';

const DropdownElement = styled.div`
  font-family: Muli;
  font-style: normal;
  font-weight: 600;
  font-size: 15px;
  line-height: normal;
  color: #58454b;
  padding: 10px 23px;
  display: block;
  text-align: left;
  cursor: pointer;

  &:hover {
    background: #f0edee;
  }
`;

export default DropdownElement;
