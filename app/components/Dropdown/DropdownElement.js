import styled from 'styled-components';

const DropdownElement = styled.div`
  font-family: Muli;
  font-style: normal;
  font-weight: 600;
  font-size: 15px;
  line-height: normal;
  color: #58454b;
  padding: 10px 0;
  display: block;
  text-align: left;
  cursor: pointer;
`;

export default DropdownElement;
