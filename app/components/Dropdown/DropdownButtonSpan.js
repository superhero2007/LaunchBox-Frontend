import styled from 'styled-components';

const DropdownButtonSpan = styled.span`
  font-family: Muli;
  font-style: normal;
  font-weight: 800;
  font-size: 15px;
  line-height: normal;
  text-align: right;
  color: ${props => props.color};
  margin-right: 1rem;
`;

export default DropdownButtonSpan;
